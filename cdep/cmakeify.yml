targets: [iOS, android]
android:
  lib: libprotobuf.a
  ndk:
    cppFlags: -DCMAKE_CXX_FLAGS="-DHAVE_PTHREAD"
    runtimes: [c++, gnustl] # stlport doesn't have tr1/unordered_map
    platforms: [12, 21]
iOS:
  lib: libprotobuf.a
example: |
   #include <sqlite3.h>
   void test() {
     sqlite3_initialize();
   }
   

